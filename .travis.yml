sudo: required

language: javascript

services: 
  - docker

before_install:
  - docker network create fyp-network
  - docker pull neo4j:3.0
  - docker pull node:argon

script:
  - cd src/test
  - npm install
  - npm test
